using TagCloud.configurations;
using TagCloud.file_readers;

namespace TagCloud.handlers
{
    public class WordsHandler
    {
        public WordsHandler(IFileReader reader, IWordFilterConfiguration configuration)
        {
            
        }
    }
}